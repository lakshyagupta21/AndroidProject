<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">
    <item android:id="@+id/action_home" android:title="Home"
        app:showAsAction="always" android:icon="@drawable/ic_home_black_24dp"
        android:showAsAction="always"/>
    <item android:id="@+id/action_register" android:title="Register"
   android:icon="@drawable/ic_edit_black_24dp"
        android:showAsAction="ifRoom|withText"/>
    <item android:id="@+id/action_search" android:title="Search"
        app:showAsAction="always" android:icon="@drawable/ic_search_black_24dp"
        android:showAsAction="always"/>

</menu>
